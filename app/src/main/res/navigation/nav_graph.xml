<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_home">

    <!-- ✅ NUOVO: Rate Fragment -->
    <fragment
        android:id="@+id/navigation_rate"
        android:name="com.example.conti.ui.rate.RateFragment"
        android:label="Rate"
        tools:layout="@layout/fragment_rate" />

    <fragment
        android:id="@+id/navigation_conti"
        android:name="com.example.conti.ui.conti.ContiFragment"
        android:label="Conti"
        tools:layout="@layout/fragment_conti">

        <action
            android:id="@+id/action_conti_to_movimenti"
            app:destination="@id/navigation_movimenti"
            app:enterAnim="@android:anim/slide_in_left"
            app:exitAnim="@android:anim/slide_out_right"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out" />
    </fragment>

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.conti.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/navigation_movimenti"
        android:name="com.example.conti.ui.movimenti.MovimentiFragment"
        android:label="Movimenti"
        tools:layout="@layout/fragment_movimenti">

        <argument
            android:name="accountId"
            app:argType="string"
            app:nullable="true"
            android:defaultValue="@null" />

        <argument
            android:name="accountName"
            app:argType="string"
            app:nullable="true"
            android:defaultValue="@null" />
    </fragment>

    <fragment
        android:id="@+id/navigation_abbonamenti"
        android:name="com.example.conti.ui.abbonamenti.AbbonamentiFragment"
        android:label="Abbonamenti"
        tools:layout="@layout/fragment_abbonamenti" />

    <!-- ✅ NUOVO: Debiti Fragment -->
    <fragment
        android:id="@+id/navigation_debiti"
        android:name="com.example.conti.ui.debiti.DebitiFragment"
        android:label="Debiti"
        tools:layout="@layout/fragment_debiti" />

</navigation>