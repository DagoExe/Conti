<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ PERMESSO INTERNET (FONDAMENTALE PER FIREBASE) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permessi per leggere file da storage esterno (Dropbox) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name="com.example.conti.ContiApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conti"
        tools:targetApi="31">

        <!-- ✅ LoginActivity - Activity di lancio -->
        <activity
            android:name="com.example.conti.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Conti"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ RegisterActivity - Activity di registrazione -->
        <activity
            android:name="com.example.conti.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.Conti"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName="com.example.conti.auth.LoginActivity">
            <!-- Meta-data per supporto navigazione back -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.conti.auth.LoginActivity" />
        </activity>

        <!-- MainActivity - Activity principale (dopo login) -->
        <activity
            android:name="com.example.conti.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Conti" />

    </application>

</manifest>